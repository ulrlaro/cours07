version: 2.1

jobs:
  sayhello:
    description: A job that says Hello
    machine: true
    steps:
      - run: echo "Hello World!"
      - run: echo "Bonjour le monde"

workflows:
  monWorkflow:
    jobs:
      - sayhello
